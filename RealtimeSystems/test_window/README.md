This folder only contains some examples to test if the development environment works appropriately.

## How to use it
sudo apt install liballegro4-dev

gcc test_allegro.c -o test `allegro-config --cflags --libs`